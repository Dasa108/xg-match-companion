# Python
__pycache__/
*.pyc
training/.venv/

# Data — large, re-downloadable via training/pull_data.py
training/data/raw/
training/data/processed/

# Training artifacts (regenerable); the released model lives in models/
training/artifacts/

# OS / editor
.DS_Store
*.swp

# Raw source art (third-party stock photo) — only the processed, muted derivative in
# app/public/img/ is checked in and shipped; this is just the working source.
/image.png
